<% if error && message && message.length > 0%>
  <div class="message"><%=message%></div>
<% end %>

<% if p %>
  <div class="box">
    <h2>URL</h2>
    <div class="boxContent"><img src="<%=p.favicon%>"/><a href="<%=p.url.to_s%>"><%=p.url.to_s%></a></div>
  </div>
  <% if p.title.length > 0 %>
  <div class="box">
    <h2>Title</h2>
    <div class="boxContent"><%=p.title%></div>
  </div>
  <% end %>
  
  <% if p.description.length > 0 %>
  <div class="box">
    <h2>Description</h2>
    <div class="boxContent"><%=p.description%></div>
  </div>  
  <% end %>
  
  <% if p.keywords.length > 0 %>
  <div class="box">
    <h2>Keywords</h2>
    <div class="boxContent"><%=p.keywords.join(", ")%></div>
  </div>
  <% end %>
  
  <% if p.image_sources.length > 0 %>
  <div class="box boxImages">
    <h2>Images</h2>
    <div class="boxContent">
      <div class="imagesContainer carousel">
        <ul>
        <% p.image_sources.each do |src| %>
          <li><a href="<%=src%>" target="_blank"<img src="<%=src%>" /></a></li>
        <% end %>
        </ul>
      </div>
      <div class="imagesButts"><a class="prevButt" href="javascript:void(0);">&laquo; previous</a><a class="nextButt" href="javascript:void(0);">next &raquo;</a></div>
    </div>
  </div>
  <% end %>
<% end %>

<% if !p && !error %>
  <div class="box">
    <h2>What is Analyze This?</h2>
    <div class="boxContent">Analyze This! is a simple service with a grand scope. It allows you to pull information about any webpage that you've provided the URL for, retrieving the page's title, favicon, description, keywords and images.<br/>
      Analyze This! joins the club of eSpace's opensource projects; you can check out its code at its <a href="http://github.com/humanzz/analyze_this">Github repository</a> and use the code to integrate the service into your own website.
    </div>
  </div>
  <div class="box">
    <h2>How do I use it?</h2>
    <div class="boxContent">The API is accessible at http://analyzethis.espace-technologies.com/js - all you need to do is provide it with the URL parameter! It returns a JSON response providing the information about the given URL  and you can provide a callback parameter as well.<br/><br/>

      Consult the <a href="http://rdoc.info/projects/humanzz/analyze_this">documentation</a> for an example on how to use the API.
    </div>
  </div>
<% end %>
