<% if error && message && message.length > 0%>
  <div class="message"><%=message%></div>
<% end %>

<% if p %>
  <div class="box">
    <h2>URL</h2>
    <div class="boxContent"><img src="<%=p.favicon%>"/><%=p.url.to_s%></div>
  </div>
  <% if p.title.length > 0 %>
  <div class="box">
    <h2>Title</h2>
    <div class="boxContent"><%=p.title%></div>
  </div>
  <% end %>
  
  <% if p.description.length > 0 %>
  <div class="box">
    <h2>Description</h2>
    <div class="boxContent"><%=p.description%></div>
  </div>  
  <% end %>
  
  <% if p.keywords.length > 0 %>
  <div class="box">
    <h2>Keywords</h2>
    <div class="boxContent"><%=p.keywords.join(", ")%></div>
  </div>
  <% end %>
  
  <% if p.image_sources.length > 0 %>
  <div class="box boxImages">
    <h2>Images</h2>
    <div class="boxContent">
      <div class="imagesContainer carousel">
        <ul>
        <% p.image_sources.each do |src| %>
          <li><a href="http://www.espn.com" target="_blank"<img src="<%=src%>" /></a></li>
        <% end %>
        </ul>
      </div>
      <div class="imagesButts"><a class="prevButt" href="javascript:void(0);">&laquo; previous</a><a class="nextButt" href="javascript:void(0);">next &raquo;</a></div>
    </div>
  </div>
  <% end %>
<% end %>

<% if !p && !error %>
  <div class="box">
    <h2>What is Analyze This?</h2>
    <div class="boxContent">It is a service that allows you to pull information about a webpage. Its get the page's title, favicon, description, keywords and images.<br/>
    It's an open source project and you can check its code at its <a href="http://github.com/humanzz/analyze_this">Github repository</a>.
    </div>
  </div>
  <div class="box">
    <h2>How do I use it?</h2>
    <div class="boxContent">The API is accessible at http://analyzethis.espace-technologies.com/js and all you need to provide it with is url parameter. It returns a JSON response providing the information about the given URL. You can provide a callback parameter as well.<br/>
    Consult the <a href="http://rdoc.info/projects/humanzz/analyze_this">documentation</a> for an example on how to use the API.
    </div>
  </div>
<% end %>
